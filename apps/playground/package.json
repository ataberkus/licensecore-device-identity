{
  "name": "@licensecore/playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Diagnostics playground for device identity. Vite, plain TS, no UI framework.",
  "scripts": {
    "dev": "vite --host",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@licensecore/client": "workspace:*",
    "@licensecore/shared": "workspace:*"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
