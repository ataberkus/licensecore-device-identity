# Local / CI secrets — copy to `.env` (never commit).
# `pnpm --filter @licensecore/server dev` falls back to testEnv if these are unset.

IP_PEPPER=dev-ip-pepper-change-me-32bytes-min
JWT_SECRET=dev-jwt-secret-change-me-32bytes-min!
ADMIN_API_KEY=dev-admin-api-key

DATABASE_DIALECT=sqlite
DATABASE_URL=file:./data/device-identity.sqlite

# PORT=8787
# WEBAUTHN_RP_ID=localhost
